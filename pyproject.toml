[project]
name = "notion-calendar-mcp"
version = "0.1.0"
description = "Stdio MCP server for Notion Calendar (undocumented calendar-api.notion.so API)."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Felipe Orlando" }]
dependencies = [
    "mcp==1.29.1",
]
